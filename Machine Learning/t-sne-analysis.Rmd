---
title: "t-sne"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
library(ggplot2)

# setwd("../data")
data <- read.csv("ML.csv", header = TRUE, stringsAsFactors=FALSE)
data <- data[,-c(1,3,4)]
data <- data[data$summary.yearbuilt > 5,]
data <- data[data$building.rooms.bathstotal > 0,]

library(psych)
pairs.panels(data, ellipses = FALSE, smooth = FALSE, cex.cor = 2)
```



```{r}
library("Rtsne")
Rtsne <- Rtsne(data, dims=2, initial_dims = 7, perplexity = 15, max_iter = 3000, 
               theta = 0.5, pca_center = TRUE, pca_scale = TRUE)
df <- as.data.frame(Rtsne$Y)
p <- ggplot(data=df, aes(x=df$V1,y=df$V2)) +
  geom_point(aes(color = data$location.distance), size = 4, alpha = 0.9) + theme_classic()
p
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
